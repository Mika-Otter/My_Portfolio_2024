{
    "name": "my-project",
    "private": true,
    "scripts": {
        "start": "concurrently \"npm run start --prefix backend --host\" \"npm run dev --prefix frontend --host\""
    },
    "devDependencies": {
        "concurrently": "^7.6.0"
    }
}
